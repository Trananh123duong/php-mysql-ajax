version: '3.8'

services:
  php:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: php-ajax-app
    ports:
      - "8085:80"
    volumes:
      - ./src:/var/www/html
    depends_on:
      - mysql

  mysql:
    image: mysql:8.0
    container_name: mysql-php-ajax
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: phptestdb
      MYSQL_USER: user
      MYSQL_PASSWORD: userpassword
    ports:
      - "33065:3306"
    volumes:
      - ./data:/var/lib/mysql
